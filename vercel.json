cat << 'EOF' > vercel.json
{
  "devCommand": "python3 servidor.py",
  "builds": [
    {
      "src": "servidor.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "servidor.py"
    }
  ]
}
EOF
